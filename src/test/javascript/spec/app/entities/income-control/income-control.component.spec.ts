import { ComponentFixture, TestBed } from '@angular/core/testing';
import { of } from 'rxjs';
import { HttpHeaders, HttpResponse } from '@angular/common/http';
import { ActivatedRoute, Data } from '@angular/router';

import { VisitTestModule } from '../../../test.module';
import { IncomeControlComponent } from 'app/entities/income-control/income-control.component';
import { IncomeControlService } from 'app/entities/income-control/income-control.service';
import { IncomeControl } from 'app/shared/model/income-control.model';

describe('Component Tests', () => {
  describe('IncomeControl Management Component', () => {
    let comp: IncomeControlComponent;
    let fixture: ComponentFixture<IncomeControlComponent>;
    let service: IncomeControlService;

    beforeEach(() => {
      TestBed.configureTestingModule({
        imports: [VisitTestModule],
        declarations: [IncomeControlComponent],
        providers: [
          {
            provide: ActivatedRoute,
            useValue: {
              data: {
                subscribe: (fn: (value: Data) => void) =>
                  fn({
                    pagingParams: {
                      predicate: 'id',
                      reverse: false,
                      page: 0
                    }
                  })
              }
            }
          }
        ]
      })
        .overrideTemplate(IncomeControlComponent, '')
        .compileComponents();

      fixture = TestBed.createComponent(IncomeControlComponent);
      comp = fixture.componentInstance;
      service = fixture.debugElement.injector.get(IncomeControlService);
    });

    it('Should call load all on init', () => {
      // GIVEN
      const headers = new HttpHeaders().append('link', 'link;link');
      spyOn(service, 'query').and.returnValue(
        of(
          new HttpResponse({
            body: [new IncomeControl(123)],
            headers
          })
        )
      );

      // WHEN
      comp.ngOnInit();

      // THEN
      expect(service.query).toHaveBeenCalled();
      expect(comp.incomeControls && comp.incomeControls[0]).toEqual(jasmine.objectContaining({ id: 123 }));
    });

    it('should load a page', () => {
      // GIVEN
      const headers = new HttpHeaders().append('link', 'link;link');
      spyOn(service, 'query').and.returnValue(
        of(
          new HttpResponse({
            body: [new IncomeControl(123)],
            headers
          })
        )
      );

      // WHEN
      comp.loadPage(1);

      // THEN
      expect(service.query).toHaveBeenCalled();
      expect(comp.incomeControls && comp.incomeControls[0]).toEqual(jasmine.objectContaining({ id: 123 }));
    });

    it('should calculate the sort attribute for an id', () => {
      // WHEN
      comp.ngOnInit();
      const result = comp.sort();

      // THEN
      expect(result).toEqual(['id,desc']);
    });

    it('should calculate the sort attribute for a non-id attribute', () => {
      // INIT
      comp.ngOnInit();

      // GIVEN
      comp.predicate = 'name';

      // WHEN
      const result = comp.sort();

      // THEN
      expect(result).toEqual(['name,desc', 'id']);
    });
  });
});
